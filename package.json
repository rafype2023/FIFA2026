{
  "name": "fifa-2026",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "start": "cd app && npm run start",
    "dev": "cd app && npm run dev"
  }
}
